# /etc/nginx/sites-available/fruitfulnetwork.com.conf

# Redirect HTTP → HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name fruitfulnetwork.com www.fruitfulnetwork.com;

    # Leave this for certbot HTTP challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    return 301 https://$host$request_uri;
}

# HTTPS server
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name fruitfulnetwork.com www.fruitfulnetwork.com;

    # --- SSL config (paths from certbot) ---
    ssl_certificate     /etc/letsencrypt/live/fruitfulnetwork.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/fruitfulnetwork.com/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    access_log /var/log/nginx/fruitfulnetwork.access.log;
    error_log  /var/log/nginx/fruitfulnetwork.error.log;

    # If you *ever* want to serve static directly, this root is handy,
    # but in this design, most traffic just goes to Flask.
    root /srv/webapps/clients/fruitfulnetwork.com/frontend;
    index index.html;

    # (Optional) Serve really heavy static assets directly from NGINX
    # location /assets/ {
    #     alias /srv/webapps/clients/fruitfulnetwork.com/frontend/assets/;
    # }

    # Everything else → shared Flask backend
    location / {
        include proxy_params;
        proxy_pass http://127.0.0.1:8000;
        proxy_redirect off;
    }
}
